# 베이스 TAB 트랜스크라이버 — 프로젝트 명세서

## 1. 프로젝트 개요

베이스 기타 음원 파일(MP3/WAV/FLAC 등)을 입력받아 음정을 자동 감지하고, 베이스 타브악보(TAB)로 변환해 화면에 표시하고 파일로 저장하는 데스크톱 앱.

- **언어:** Python 3
- **GUI 프레임워크:** CustomTkinter (다크 테마)
- **진입점:** `main.py` → `src/app.py`
- **실행:** `bash run.sh` 또는 `python main.py`

---

## 2. 아키텍처 — 모듈 구성

```
Bass/
├── main.py                  # 진입점
├── requirements.txt         # 의존성
├── run.sh                   # 실행 스크립트
└── src/
    ├── app.py               # GUI 메인 (BassTabApp)
    ├── audio_processor.py   # 오디오 로딩 및 전처리
    ├── pitch_detector.py    # 음정 감지 (Basic Pitch / librosa)
    ├── fretboard_mapper.py  # MIDI → 프렛 위치 변환
    ├── tab_renderer.py      # ASCII TAB 텍스트 렌더링
    └── tab_canvas.py        # 그래픽 TAB 캔버스 위젯
```

### 데이터 흐름

```
오디오 파일
    │
    ▼
AudioProcessor.load()          # librosa로 로드 → (y, sr)
    │
    ▼
PitchDetector.detect_notes()   # 음정 감지 → List[NoteEvent]
    │
    ▼
FretboardMapper.map_notes()    # 프렛 매핑 → List[TabNote]
    │
    ├──▶ TabCanvas.set_notes()  # 그래픽 악보 표시
    └──▶ TabRenderer.render_*() # ASCII TAB 텍스트 생성
              │
              └──▶ 내보내기 (TXT / PDF / PNG)
```

---

## 3. 모듈 상세 명세

### 3-1. `audio_processor.py` — AudioProcessor

| 항목 | 내용 |
|------|------|
| 지원 형식 | `.mp3` `.wav` `.flac` `.m4a` `.ogg` `.aiff` |
| 샘플링 레이트 | 22,050 Hz (mono 리샘플링) |
| 전처리 | 진폭 정규화 → librosa harmonic 분리 (margin=3.0) |

**주요 메서드**
- `load(file_path)` → `(y: ndarray, sr: int)`
- `preprocess_bass(y, sr)` → 하모닉 성분만 추출된 `y`
- `get_duration(y, sr)` → 초 단위 길이

---

### 3-2. `pitch_detector.py` — PitchDetector

**감지 범위**

| 변수 | 값 | 의미 |
|------|----|------|
| `BASS_MIDI_MIN` | 28 | E1 (41.2 Hz) |
| `BASS_MIDI_MAX` | 55 | G3 (196 Hz) |
| `BASS_HZ_MIN` | 35.0 Hz | |
| `BASS_HZ_MAX` | 220.0 Hz | |

**감지 방법 (`method` 파라미터)**

| 값 | 동작 |
|----|------|
| `'auto'` | CREPE → Basic Pitch → librosa 순으로 자동 시도 |
| `'crepe'` | CREPE 딥러닝 F0 추정 강제 사용 |
| `'basic-pitch'` | Spotify Basic Pitch ONNX 모델 강제 사용 |
| `'librosa'` | librosa pyin 강제 사용 |

**CREPE 백엔드 (권장)**
- 딥러닝 기반 단음 F0 추정기 (Kereliuk et al., 2018)
- 샘플링 레이트: 16,000 Hz (CREPE 권장)
- viterbi=True → 옥타브 점프 억제
- step_size=10ms → 세밀한 프레임 해상도
- voiced 임계값: confidence > 0.45
- 설치: `pip install crepe`

**Basic Pitch 백엔드**
- ONNX 모델 경로: `basic_pitch/saved_models/icassp_2022/nmp.onnx`
- TensorFlow 불필요 (onnxruntime만 필요)
- 후처리: 옥타브 중복 제거 → 같은 음정 병합 → 짧은 노트 필터

**librosa 백엔드**
- 알고리즘: `pyin` (HOP=256, FRAME=4096)
- 전처리: 28–380 Hz 밴드패스 필터 → 하모닉 분리
- 후처리: f0 스무딩(median) → 온셋 기반 노트 분할 → 병합/필터

**NoteEvent 데이터 클래스**

```python
@dataclass
class NoteEvent:
    pitch_midi: int
    start_time: float   # 초
    end_time:   float   # 초
    velocity:   float   # 0.0–1.0
    confidence: float   # 0.0–1.0
```

---

### 3-3. `fretboard_mapper.py` — FretboardMapper

**지원 조율**

| 키 | 튜닝 | 개방현 MIDI |
|----|------|------------|
| `standard` | G D A E | 43 38 33 28 |
| `drop_d` | G D A D | 43 38 33 26 |
| `standard_5` | G D A E B (5현) | 43 38 33 28 23 |
| `drop_d_5` | G D A E A (5현) | 43 38 33 28 21 |

**매핑 알고리즘 (Greedy)**
- 초기 핸드 포지션: 프렛 2
- 각 음표마다 가능한 (현, 프렛) 후보 중 코스트 최솟값 선택
- "이전에 잡은 프렛에서 가장 가까운 프렛" 로직: `hand_fret` 트래킹으로 구현 (기본 내장)
- 코스트 함수:
  - 핸드 포지션 이동 거리 (주요 인자)
  - 낮은 프렛 번호 선호 (× 0.08)
  - 개방현 보너스 (−2.0)
  - **E (idx 3) · A (idx 2) 현 선호** — G·D 현에 +0.30 페널티 (베이스 자연 운지 반영)

**TabNote 데이터 클래스**

```python
@dataclass
class TabNote:
    string_idx: int   # 0=G, 1=D, 2=A, 3=E
    fret: int
    start_time: float
    end_time: float
    pitch_midi: int
```

---

### 3-4. `tab_renderer.py` — TabRenderer

**render_measures()** — BPM 기반 마디 정렬 TAB

| 파라미터 | 기본값 | 설명 |
|----------|--------|------|
| `bpm` | 120.0 | 템포 |
| `beats_per_measure` | 4 | 박자표 분자 (4/4) |
| `measures_per_row` | 4 | 행당 마디 수 |
| `resolution` | 8 | 박당 그리드 세분화 |

출력 예시:
```
G|---5---7---------|---5---7---------|
D|-----------------|-5---7-----------|
A|-3---3---3---3---|-----------------|
E|-----------------|-----------------|
  1                 2
```

**render_simple()** — 시간 비례 TAB (루바토 구간용)

**note_list()** — 음표 목록 (디버깅용, 시간/현/프렛/음이름 표시)

---

### 3-5. `tab_canvas.py` — TabCanvas

tkinter Canvas 기반 그래픽 TAB 위젯.

**레이아웃 상수**

| 상수 | 값 | 의미 |
|------|----|------|
| `PX_PER_BEAT` | 56px | 기본 1박 너비 (동적 조정됨) |
| `MIN_NOTE_GAP_PX` | 22px | 노트 간 최소 픽셀 간격 |
| `STR_SPACING` | 46px | 현 간격 |
| `LEFT_MARGIN` | 54px | 현 이름 영역 |
| `BEATS_PER_BAR` | 4 | 마디당 박 |

**동적 간격 알고리즘 (`_compute_px_per_beat`)**
1. 음표들의 박자 위치를 정렬해 최소 박 간격(`min_gap`) 계산
2. `MIN_NOTE_GAP_PX / min_gap` 으로 필요한 `px_per_beat` 산출
3. 기본값(56px)과 최댓값(300px) 사이로 클램프
4. 음표가 밀집할수록 악보가 가로로 확장되어 겹침 방지

**현별 노트 색상 (텍스트 전용, 박스 없음)**

| 현 | 텍스트 색 |
|----|----------|
| G | #42A5F5 (하늘) |
| D | #66BB6A (초록) |
| A | #FF9800 (주황) |
| E | #CE93D8 (보라) |

노트는 박스 없이 텍스트만 표시. 현 선 위 텍스트 영역을 BG색으로 클리어해 가독성 확보.

**주요 기능**
- 수직/수평 스크롤바
- 마우스 휠 스크롤
- `export_image(path)` → PNG 저장 (PIL 필요)

---

## 4. GUI 명세 — BassTabApp

### 4-1. 툴바

| 컨트롤 | 기능 |
|--------|------|
| 파일 열기 버튼 | 파일 다이얼로그로 오디오 파일 선택 |
| 분석 버튼 | 비동기 스레드로 분석 시작 |
| 저장 버튼 | TXT / PDF / PNG 내보내기 |
| BPM 입력 | 템포 설정 (기본 120) |
| **BPM 자동 버튼** | librosa.beat.beat_track으로 BPM 자동 감지 후 입력창에 반영 |
| 보기 선택 | 악보 / 음표 목록 / 텍스트 TAB 전환 |

### 4-2. 왼쪽 패널 (설정)

| 설정 | 옵션 |
|------|------|
| 파일 드롭 영역 | 드래그&드롭 (tkinterdnd2 있을 때만 활성) |
| 감지 방법 | auto / librosa / basic-pitch / **crepe** |
| 조율 | standard / drop_d / standard_5 / drop_d_5 |
| 신뢰도 임계값 | 슬라이더 0.0 – 1.0 (기본 0.25) |
| 마디/행 | 2 / 3 / 4 / 6 / 8 (기본 4) |

### 4-3. 오른쪽 패널 (TAB 표시)

- 기본 보기: 그래픽 악보 (TabCanvas)
- 보기 전환 시 텍스트박스 표시 (Courier New 12pt)

---

## 5. 의존성

| 패키지 | 용도 |
|--------|------|
| `customtkinter >= 5.2.0` | GUI 프레임워크 |
| `librosa >= 0.10.0` | 오디오 로딩, pyin 음정 감지 |
| `numpy >= 1.24.0` | 배열 연산 |
| `soundfile >= 0.12.0` | 오디오 파일 I/O |
| `reportlab >= 4.0.0` | PDF 내보내기 |
| `crepe` *(선택, 권장)* | 딥러닝 F0 추정 (가장 정확) |
| `basic-pitch` *(선택)* | AI 기반 정확한 음정 감지 |
| `onnxruntime` *(선택)* | Basic Pitch ONNX 모델 실행 |
| `tkinterdnd2` *(선택)* | 드래그&드롭 지원 |
| `pillow` *(선택)* | PNG 내보내기 |

---

## 6. 내보내기 형식

| 형식 | 내용 |
|------|------|
| `.txt` | ASCII TAB 텍스트 (UTF-8) |
| `.pdf` | ReportLab A4 문서, Courier 7pt 모노스페이스 |
| `.png` | TabCanvas 전체 렌더링 이미지 (PIL 필요) |

---

## 7. 알려진 이슈 / 제약

- macOS 26 에서 Tkinter 관련 호환성 이슈 가능성 있음
- Basic Pitch 없을 경우 librosa pyin으로 자동 전환되며 정확도 저하
- 5현 베이스 조율 시 `STRING_NAMES` 인덱스가 4현 기준(`['G','D','A','E']`)으로 하드코딩된 부분 혼용 가능성 있음 (`tab_renderer.py` 67번째 줄)
- PNG 내보내기는 `pillow` 미설치 시 런타임 오류 발생

---

## 8. 분석 처리 흐름 (상세)

```
1. 파일 로드           AudioProcessor → librosa.load (22050Hz mono)
2. 음정 감지 (10~65%)  PitchDetector
   ├─ Basic Pitch    ONNX 추론 → MIDI 이벤트 → 중복/병합/필터
   └─ librosa pyin   밴드패스 → 하모닉 → 온셋 검출 → pyin → 스무딩 → 노트 생성
3. 프렛 매핑 (65~85%) FretboardMapper → Greedy 알고리즘
4. 렌더링 (85~100%)   TabRenderer (텍스트) + TabCanvas (그래픽)
5. UI 업데이트        after(0, callback) — 메인 스레드에 위임
```
